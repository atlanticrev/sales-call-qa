# Sales Call QA — анализ качества телефонных звонков

Этот проект автоматически:

1. принимает аудиозапись телефонного звонка (менеджер ↔ клиент)
2. превращает речь в текст
3. определяет, кто говорит (менеджер / клиент)
4. анализирует качество продажи с помощью ИИ
5. формирует простой PDF-отчёт

Работает локально, бесплатно, без регистрации в сервисах.

---

## 🧠 Что это вообще такое (простыми словами)

- **Docker** — запускает всё одной командой  
- **Whisper** — переводит речь в текст  
- **Ollama** — анализирует разговор (ИИ)  
- **Node.js** — связывает всё вместе  
- **PDF** — результат для просмотра и отправки  

Тебе **НЕ нужно**:
- знать программирование
- знать Docker
- знать Python или Node.js

---

## 🧱 Из чего состоит проект

Проект запускает 3 программы одновременно:

1. **API** — принимает аудио и отдаёт PDF  
2. **STT** — распознаёт речь  
3. **LLM** — анализирует разговор  

Docker управляет всем автоматически.

---

## ✅ Что нужно заранее

### 1️⃣ Установить Docker

- **Mac / Windows** → Docker Desktop  
- **Linux** → Docker Engine  

Проверка:
```bash
docker --version
docker compose version
```

---

### 2️⃣ Получить HF_TOKEN (1 раз)

Нужен, чтобы определять, **кто говорит в разговоре**.

1. Зайти на https://huggingface.co  
2. Зарегистрироваться  
3. Settings → Access Tokens  
4. Create new token  
5. Принять условия модели `pyannote/speaker-diarization`  
6. Скопировать токен (начинается с `hf_...`)  

---

## 🚀 Как запустить проект (ПОШАГОВО)

### Шаг 1. Зайти в папку проекта

```bash
cd sales-call-qa
```

---

### Шаг 2. Создать файл `.env`

```bash
cp .env.example .env
```

Открыть `.env` и вставить токен:

```env
HF_TOKEN=hf_xxxxxxxxxxxxxxxxx
```

---

### Шаг 3. Запустить Docker

```bash
docker compose up --build
```

⏳ **Первый запуск может занять 5–15 минут**  
(скачиваются модели и зависимости)

Оставь терминал открытым.

---

### Шаг 4. Скачать модель ИИ (ОБЯЗАТЕЛЬНО, 1 раз)

В новом терминале:

```bash
docker exec -it sales-ollama ollama pull qwen2.5:7b
```

Проверка:
```bash
docker exec -it sales-ollama ollama list
```

---

## 📞 Как пользоваться

Отправь аудиофайл и получи PDF-отчёт.

Поддерживаются:
- `.wav`
- `.mp3`

Пример:
```bash
curl -F "file=@call.wav" http://localhost:3000/process --output report.pdf
```

После этого появится файл `report.pdf`.

---

## 📄 Что есть в PDF

- итоговая оценка звонка (0–10)
- краткое резюме
- таблица критериев
- ошибки менеджера
- рекомендации
- полный текст разговора с таймкодами

---

## ⏹ Как остановить проект

```bash
docker compose down
```

---

## 🔁 Как запустить снова

```bash
docker compose up
```

(модели скачиваться повторно не будут)

---

## 🗑 Как удалить всё полностью

```bash
docker compose down -v
```

---

## ❌ Если что-то не работает

Посмотреть логи:
```bash
docker compose logs -f
```

Частые причины:
- Docker Desktop не запущен
- забыли выполнить `ollama pull`
- забыли HF_TOKEN
- порт 3000 занят

---

## 🧠 Частые вопросы

**Нужно ли знать программирование?**  
Нет.

**Это бесплатно?**  
Да, полностью.

**Работает ли без интернета?**  
Да, после первого запуска.

**Можно ли дать это другу?**  
Да. Просто передай проект и этот README.

---

## 🏁 Кратко

1. Установи Docker  
2. Вставь HF_TOKEN  
3. `docker compose up`  
4. `ollama pull qwen2.5:7b`  
5. Отправь аудио → получи PDF  

Готово 🚀
